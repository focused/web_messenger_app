p
  = t('.companions', names: @chat.companion_list)

= link_to t('index.title', scope: 'messenger.chats'), chats_path

br

#message_list
  = render @chat.messages

br

#new_message_container
  = render 'messenger/messages/form', message: @chat.new_message

= subscribe_to "/chats/#{@chat.id}"
